import React from "react";
import ReactDOM from "react-dom/client";
import TrustLayerDemo from "./TrustLayerDemo.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TrustLayerDemo />
  </React.StrictMode>
);
